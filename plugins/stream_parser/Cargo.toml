[package]
name = "plugin-stream-parser"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "ZS-AI 流式解析插件：SSE (Server-Sent Events) 解析纯函数"

[lib]
crate-type = ["cdylib"]

[dependencies]
zsai-core = { path = "../../core" }
serde_json = "1"
tracing = "0.1"
