[package]
name = "plugin-character-card"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "ZS-AI 角色卡插件：SillyTavern v2/v3 PNG 解析与 JSON 加载"

[lib]
crate-type = ["cdylib"]

[dependencies]
zsai-core = { path = "../../core" }
serde_json = "1"
tracing = "0.1"
