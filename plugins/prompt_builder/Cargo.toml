[package]
name = "plugin-prompt-builder"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "ZS-AI 提示词构建插件：从角色卡 JSON 组装 System Prompt"

[lib]
crate-type = ["cdylib"]

[dependencies]
zsai-core = { path = "../../core" }
serde_json = "1"
tracing = "0.1"
