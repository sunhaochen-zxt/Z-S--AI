[package]
name = "plugin-token-counter"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "ZS-AI Token 计数插件：估算对话 token 用量，触发上下文裁剪"

[lib]
crate-type = ["cdylib"]

[dependencies]
zsai-core = { path = "../../core" }
serde_json = "1"
tracing = "0.1"
