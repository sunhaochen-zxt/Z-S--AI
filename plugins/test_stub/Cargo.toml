[package]
name = "plugin-test-stub"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "ZS-AI 测试桩插件：验证插件系统加载/执行链路"

[lib]
crate-type = ["cdylib"]

[dependencies]
zsai-core = { path = "../../core" }
serde_json = "1"
tracing = "0.1"
